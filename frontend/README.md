# Frontend
## Local development
From the repo root:
```sh
cd frontend
npm install
npm run dev
```
The Vite dev server runs on port `8080` by default. If that port is taken, Vite will pick another available port and print it in the console.
## Tech
- Vite
- TypeScript
- React
- shadcn/ui
- Tailwind CSS